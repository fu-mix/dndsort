import * as React from 'react';
import dayjs from 'dayjs';
import { Typography } from '@material-ui/core';

export const CalendarElement: React.FC = () => {
  const today = dayjs();
  return <div>col test</div>;
};
